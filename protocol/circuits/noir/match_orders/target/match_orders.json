{"noir_version":"1.0.0-beta.9+6abff2f16e1c1314ba30708d1cf032a536de3d19","hash":"11635747780933727241","abi":{"parameters":[{"name":"order_commitment_a","type":{"kind":"field"},"visibility":"public"},{"name":"order_commitment_b","type":{"kind":"field"},"visibility":"public"},{"name":"fill_note_buyer","type":{"kind":"field"},"visibility":"public"},{"name":"fill_note_seller","type":{"kind":"field"},"visibility":"public"},{"name":"residual_order_a","type":{"kind":"field"},"visibility":"public"},{"name":"residual_order_b","type":{"kind":"field"},"visibility":"public"},{"name":"refund_note_a","type":{"kind":"field"},"visibility":"public"},{"name":"refund_note_b","type":{"kind":"field"},"visibility":"public"},{"name":"a_side","type":{"kind":"field"},"visibility":"private"},{"name":"a_price","type":{"kind":"field"},"visibility":"private"},{"name":"a_amount","type":{"kind":"field"},"visibility":"private"},{"name":"a_asset_base","type":{"kind":"field"},"visibility":"private"},{"name":"a_asset_quote","type":{"kind":"field"},"visibility":"private"},{"name":"a_owner_key","type":{"kind":"field"},"visibility":"private"},{"name":"a_nonce","type":{"kind":"field"},"visibility":"private"},{"name":"b_side","type":{"kind":"field"},"visibility":"private"},{"name":"b_price","type":{"kind":"field"},"visibility":"private"},{"name":"b_amount","type":{"kind":"field"},"visibility":"private"},{"name":"b_asset_base","type":{"kind":"field"},"visibility":"private"},{"name":"b_asset_quote","type":{"kind":"field"},"visibility":"private"},{"name":"b_owner_key","type":{"kind":"field"},"visibility":"private"},{"name":"b_nonce","type":{"kind":"field"},"visibility":"private"},{"name":"buyer_fill_blinding","type":{"kind":"field"},"visibility":"private"},{"name":"seller_fill_blinding","type":{"kind":"field"},"visibility":"private"},{"name":"residual_a_nonce","type":{"kind":"field"},"visibility":"private"},{"name":"residual_b_nonce","type":{"kind":"field"},"visibility":"private"},{"name":"refund_a_blinding","type":{"kind":"field"},"visibility":"private"},{"name":"refund_b_blinding","type":{"kind":"field"},"visibility":"private"}],"return_type":null,"error_types":{"1122129203405668964":{"error_kind":"string","string":"residual B mismatch"},"2543161216383961521":{"error_kind":"string","string":"refund A mismatch"},"3218549388860021571":{"error_kind":"string","string":"base asset mismatch"},"5019202896831570965":{"error_kind":"string","string":"attempt to add with overflow"},"5811533743894596360":{"error_kind":"string","string":"residual A mismatch"},"6485997221020871071":{"error_kind":"string","string":"call to assert_max_bit_size"},"7233212735005103307":{"error_kind":"string","string":"attempt to multiply with overflow"},"9151535435766009021":{"error_kind":"string","string":"refund B mismatch"},"10568377212091016540":{"error_kind":"string","string":"seller fill note mismatch"},"10854417585274566599":{"error_kind":"string","string":"orders must be opposite sides"},"11397323637488717055":{"error_kind":"string","string":"B side must be 0 or 1"},"11860755990325542684":{"error_kind":"string","string":"A side must be 0 or 1"},"12066925015324983175":{"error_kind":"string","string":"order B commitment mismatch"},"13583046776184195537":{"error_kind":"string","string":"buyer fill note mismatch"},"14082732712853239796":{"error_kind":"string","string":"order A commitment mismatch"},"17763767110313498300":{"error_kind":"string","string":"prices incompatible"},"18160294289518494210":{"error_kind":"string","string":"quote asset mismatch"}}},"bytecode":"","debug_symbols":"","file_map":{"18":{"source":"pub mod bn254;\nuse crate::{runtime::is_unconstrained, static_assert};\nuse bn254::lt as bn254_lt;\n\nimpl Field {\n    /// Asserts that `self` can be represented in `bit_size` bits.\n    ///\n    /// # Failures\n    /// Causes a constraint failure for `Field` values exceeding `2^{bit_size}`.\n    // docs:start:assert_max_bit_size\n    pub fn assert_max_bit_size<let BIT_SIZE: u32>(self) {\n        // docs:end:assert_max_bit_size\n        static_assert(\n            BIT_SIZE < modulus_num_bits() as u32,\n            \"BIT_SIZE must be less than modulus_num_bits\",\n        );\n        __assert_max_bit_size(self, BIT_SIZE);\n    }\n\n    /// Decomposes `self` into its little endian bit decomposition as a `[u1; N]` array.\n    /// This slice will be zero padded should not all bits be necessary to represent `self`.\n    ///\n    /// # Failures\n    /// Causes a constraint failure for `Field` values exceeding `2^N` as the resulting slice will not\n    /// be able to represent the original `Field`.\n    ///\n    /// # Safety\n    /// The bit decomposition returned is canonical and is guaranteed to not overflow the modulus.\n    // docs:start:to_le_bits\n    pub fn to_le_bits<let N: u32>(self: Self) -> [u1; N] {\n        // docs:end:to_le_bits\n        let bits = __to_le_bits(self);\n\n        if !is_unconstrained() {\n            // Ensure that the byte decomposition does not overflow the modulus\n            let p = modulus_le_bits();\n            assert(bits.len() <= p.len());\n            let mut ok = bits.len() != p.len();\n            for i in 0..N {\n                if !ok {\n                    if (bits[N - 1 - i] != p[N - 1 - i]) {\n                        assert(p[N - 1 - i] == 1);\n                        ok = true;\n                    }\n                }\n            }\n            assert(ok);\n        }\n        bits\n    }\n\n    /// Decomposes `self` into its big endian bit decomposition as a `[u1; N]` array.\n    /// This array will be zero padded should not all bits be necessary to represent `self`.\n    ///\n    /// # Failures\n    /// Causes a constraint failure for `Field` values exceeding `2^N` as the resulting slice will not\n    /// be able to represent the original `Field`.\n    ///\n    /// # Safety\n    /// The bit decomposition returned is canonical and is guaranteed to not overflow the modulus.\n    // docs:start:to_be_bits\n    pub fn to_be_bits<let N: u32>(self: Self) -> [u1; N] {\n        // docs:end:to_be_bits\n        let bits = __to_be_bits(self);\n\n        if !is_unconstrained() {\n            // Ensure that the decomposition does not overflow the modulus\n            let p = modulus_be_bits();\n            assert(bits.len() <= p.len());\n            let mut ok = bits.len() != p.len();\n            for i in 0..N {\n                if !ok {\n                    if (bits[i] != p[i]) {\n                        assert(p[i] == 1);\n                        ok = true;\n                    }\n                }\n            }\n            assert(ok);\n        }\n        bits\n    }\n\n    /// Decomposes `self` into its little endian byte decomposition as a `[u8;N]` array\n    /// This array will be zero padded should not all bytes be necessary to represent `self`.\n    ///\n    /// # Failures\n    ///  The length N of the array must be big enough to contain all the bytes of the 'self',\n    ///  and no more than the number of bytes required to represent the field modulus\n    ///\n    /// # Safety\n    /// The result is ensured to be the canonical decomposition of the field element\n    // docs:start:to_le_bytes\n    pub fn to_le_bytes<let N: u32>(self: Self) -> [u8; N] {\n        // docs:end:to_le_bytes\n        static_assert(\n            N <= modulus_le_bytes().len(),\n            \"N must be less than or equal to modulus_le_bytes().len()\",\n        );\n        // Compute the byte decomposition\n        let bytes = self.to_le_radix(256);\n\n        if !is_unconstrained() {\n            // Ensure that the byte decomposition does not overflow the modulus\n            let p = modulus_le_bytes();\n            assert(bytes.len() <= p.len());\n            let mut ok = bytes.len() != p.len();\n            for i in 0..N {\n                if !ok {\n                    if (bytes[N - 1 - i] != p[N - 1 - i]) {\n                        assert(bytes[N - 1 - i] < p[N - 1 - i]);\n                        ok = true;\n                    }\n                }\n            }\n            assert(ok);\n        }\n        bytes\n    }\n\n    /// Decomposes `self` into its big endian byte decomposition as a `[u8;N]` array of length required to represent the field modulus\n    /// This array will be zero padded should not all bytes be necessary to represent `self`.\n    ///\n    /// # Failures\n    ///  The length N of the array must be big enough to contain all the bytes of the 'self',\n    ///  and no more than the number of bytes required to represent the field modulus\n    ///\n    /// # Safety\n    /// The result is ensured to be the canonical decomposition of the field element\n    // docs:start:to_be_bytes\n    pub fn to_be_bytes<let N: u32>(self: Self) -> [u8; N] {\n        // docs:end:to_be_bytes\n        static_assert(\n            N <= modulus_le_bytes().len(),\n            \"N must be less than or equal to modulus_le_bytes().len()\",\n        );\n        // Compute the byte decomposition\n        let bytes = self.to_be_radix(256);\n\n        if !is_unconstrained() {\n            // Ensure that the byte decomposition does not overflow the modulus\n            let p = modulus_be_bytes();\n            assert(bytes.len() <= p.len());\n            let mut ok = bytes.len() != p.len();\n            for i in 0..N {\n                if !ok {\n                    if (bytes[i] != p[i]) {\n                        assert(bytes[i] < p[i]);\n                        ok = true;\n                    }\n                }\n            }\n            assert(ok);\n        }\n        bytes\n    }\n\n    fn to_le_radix<let N: u32>(self: Self, radix: u32) -> [u8; N] {\n        // Brillig does not need an immediate radix\n        if !crate::runtime::is_unconstrained() {\n            static_assert(1 < radix, \"radix must be greater than 1\");\n            static_assert(radix <= 256, \"radix must be less than or equal to 256\");\n            static_assert(radix & (radix - 1) == 0, \"radix must be a power of 2\");\n        }\n        __to_le_radix(self, radix)\n    }\n\n    fn to_be_radix<let N: u32>(self: Self, radix: u32) -> [u8; N] {\n        // Brillig does not need an immediate radix\n        if !crate::runtime::is_unconstrained() {\n            static_assert(1 < radix, \"radix must be greater than 1\");\n            static_assert(radix <= 256, \"radix must be less than or equal to 256\");\n            static_assert(radix & (radix - 1) == 0, \"radix must be a power of 2\");\n        }\n        __to_be_radix(self, radix)\n    }\n\n    // Returns self to the power of the given exponent value.\n    // Caution: we assume the exponent fits into 32 bits\n    // using a bigger bit size impacts negatively the performance and should be done only if the exponent does not fit in 32 bits\n    pub fn pow_32(self, exponent: Field) -> Field {\n        let mut r: Field = 1;\n        let b: [u1; 32] = exponent.to_le_bits();\n\n        for i in 1..33 {\n            r *= r;\n            r = (b[32 - i] as Field) * (r * self) + (1 - b[32 - i] as Field) * r;\n        }\n        r\n    }\n\n    // Parity of (prime) Field element, i.e. sgn0(x mod p) = 0 if x `elem` {0, ..., p-1} is even, otherwise sgn0(x mod p) = 1.\n    pub fn sgn0(self) -> u1 {\n        self as u1\n    }\n\n    pub fn lt(self, another: Field) -> bool {\n        if crate::compat::is_bn254() {\n            bn254_lt(self, another)\n        } else {\n            lt_fallback(self, another)\n        }\n    }\n\n    /// Convert a little endian byte array to a field element.\n    /// If the provided byte array overflows the field modulus then the Field will silently wrap around.\n    pub fn from_le_bytes<let N: u32>(bytes: [u8; N]) -> Field {\n        static_assert(\n            N <= modulus_le_bytes().len(),\n            \"N must be less than or equal to modulus_le_bytes().len()\",\n        );\n        let mut v = 1;\n        let mut result = 0;\n\n        for i in 0..N {\n            result += (bytes[i] as Field) * v;\n            v = v * 256;\n        }\n        result\n    }\n\n    /// Convert a big endian byte array to a field element.\n    /// If the provided byte array overflows the field modulus then the Field will silently wrap around.\n    pub fn from_be_bytes<let N: u32>(bytes: [u8; N]) -> Field {\n        let mut v = 1;\n        let mut result = 0;\n\n        for i in 0..N {\n            result += (bytes[N - 1 - i] as Field) * v;\n            v = v * 256;\n        }\n        result\n    }\n}\n\n#[builtin(apply_range_constraint)]\nfn __assert_max_bit_size(value: Field, bit_size: u32) {}\n\n// `_radix` must be less than 256\n#[builtin(to_le_radix)]\nfn __to_le_radix<let N: u32>(value: Field, radix: u32) -> [u8; N] {}\n\n// `_radix` must be less than 256\n#[builtin(to_be_radix)]\nfn __to_be_radix<let N: u32>(value: Field, radix: u32) -> [u8; N] {}\n\n/// Decomposes `self` into its little endian bit decomposition as a `[u1; N]` array.\n/// This slice will be zero padded should not all bits be necessary to represent `self`.\n///\n/// # Failures\n/// Causes a constraint failure for `Field` values exceeding `2^N` as the resulting slice will not\n/// be able to represent the original `Field`.\n///\n/// # Safety\n/// Values of `N` equal to or greater than the number of bits necessary to represent the `Field` modulus\n/// (e.g. 254 for the BN254 field) allow for multiple bit decompositions. This is due to how the `Field` will\n/// wrap around due to overflow when verifying the decomposition.\n#[builtin(to_le_bits)]\nfn __to_le_bits<let N: u32>(value: Field) -> [u1; N] {}\n\n/// Decomposes `self` into its big endian bit decomposition as a `[u1; N]` array.\n/// This array will be zero padded should not all bits be necessary to represent `self`.\n///\n/// # Failures\n/// Causes a constraint failure for `Field` values exceeding `2^N` as the resulting slice will not\n/// be able to represent the original `Field`.\n///\n/// # Safety\n/// Values of `N` equal to or greater than the number of bits necessary to represent the `Field` modulus\n/// (e.g. 254 for the BN254 field) allow for multiple bit decompositions. This is due to how the `Field` will\n/// wrap around due to overflow when verifying the decomposition.\n#[builtin(to_be_bits)]\nfn __to_be_bits<let N: u32>(value: Field) -> [u1; N] {}\n\n#[builtin(modulus_num_bits)]\npub comptime fn modulus_num_bits() -> u64 {}\n\n#[builtin(modulus_be_bits)]\npub comptime fn modulus_be_bits() -> [u1] {}\n\n#[builtin(modulus_le_bits)]\npub comptime fn modulus_le_bits() -> [u1] {}\n\n#[builtin(modulus_be_bytes)]\npub comptime fn modulus_be_bytes() -> [u8] {}\n\n#[builtin(modulus_le_bytes)]\npub comptime fn modulus_le_bytes() -> [u8] {}\n\n/// An unconstrained only built in to efficiently compare fields.\n#[builtin(field_less_than)]\nunconstrained fn __field_less_than(x: Field, y: Field) -> bool {}\n\npub(crate) unconstrained fn field_less_than(x: Field, y: Field) -> bool {\n    __field_less_than(x, y)\n}\n\n// Convert a 32 byte array to a field element by modding\npub fn bytes32_to_field(bytes32: [u8; 32]) -> Field {\n    // Convert it to a field element\n    let mut v = 1;\n    let mut high = 0 as Field;\n    let mut low = 0 as Field;\n\n    for i in 0..16 {\n        high = high + (bytes32[15 - i] as Field) * v;\n        low = low + (bytes32[16 + 15 - i] as Field) * v;\n        v = v * 256;\n    }\n    // Abuse that a % p + b % p = (a + b) % p and that low < p\n    low + high * v\n}\n\nfn lt_fallback(x: Field, y: Field) -> bool {\n    if is_unconstrained() {\n        // Safety: unconstrained context\n        unsafe {\n            field_less_than(x, y)\n        }\n    } else {\n        let x_bytes: [u8; 32] = x.to_le_bytes();\n        let y_bytes: [u8; 32] = y.to_le_bytes();\n        let mut x_is_lt = false;\n        let mut done = false;\n        for i in 0..32 {\n            if (!done) {\n                let x_byte = x_bytes[32 - 1 - i] as u8;\n                let y_byte = y_bytes[32 - 1 - i] as u8;\n                let bytes_match = x_byte == y_byte;\n                if !bytes_match {\n                    x_is_lt = x_byte < y_byte;\n                    done = true;\n                }\n            }\n        }\n        x_is_lt\n    }\n}\n\nmod tests {\n    use crate::{panic::panic, runtime};\n    use super::field_less_than;\n\n    #[test]\n    // docs:start:to_be_bits_example\n    fn test_to_be_bits() {\n        let field = 2;\n        let bits: [u1; 8] = field.to_be_bits();\n        assert_eq(bits, [0, 0, 0, 0, 0, 0, 1, 0]);\n    }\n    // docs:end:to_be_bits_example\n\n    #[test]\n    // docs:start:to_le_bits_example\n    fn test_to_le_bits() {\n        let field = 2;\n        let bits: [u1; 8] = field.to_le_bits();\n        assert_eq(bits, [0, 1, 0, 0, 0, 0, 0, 0]);\n    }\n    // docs:end:to_le_bits_example\n\n    #[test]\n    // docs:start:to_be_bytes_example\n    fn test_to_be_bytes() {\n        let field = 2;\n        let bytes: [u8; 8] = field.to_be_bytes();\n        assert_eq(bytes, [0, 0, 0, 0, 0, 0, 0, 2]);\n        assert_eq(Field::from_be_bytes::<8>(bytes), field);\n    }\n    // docs:end:to_be_bytes_example\n\n    #[test]\n    // docs:start:to_le_bytes_example\n    fn test_to_le_bytes() {\n        let field = 2;\n        let bytes: [u8; 8] = field.to_le_bytes();\n        assert_eq(bytes, [2, 0, 0, 0, 0, 0, 0, 0]);\n        assert_eq(Field::from_le_bytes::<8>(bytes), field);\n    }\n    // docs:end:to_le_bytes_example\n\n    #[test]\n    // docs:start:to_be_radix_example\n    fn test_to_be_radix() {\n        // 259, in base 256, big endian, is [1, 3].\n        // i.e. 3 * 256^0 + 1 * 256^1\n        let field = 259;\n\n        // The radix (in this example, 256) must be a power of 2.\n        // The length of the returned byte array can be specified to be\n        // >= the amount of space needed.\n        let bytes: [u8; 8] = field.to_be_radix(256);\n        assert_eq(bytes, [0, 0, 0, 0, 0, 0, 1, 3]);\n        assert_eq(Field::from_be_bytes::<8>(bytes), field);\n    }\n    // docs:end:to_be_radix_example\n\n    #[test]\n    // docs:start:to_le_radix_example\n    fn test_to_le_radix() {\n        // 259, in base 256, little endian, is [3, 1].\n        // i.e. 3 * 256^0 + 1 * 256^1\n        let field = 259;\n\n        // The radix (in this example, 256) must be a power of 2.\n        // The length of the returned byte array can be specified to be\n        // >= the amount of space needed.\n        let bytes: [u8; 8] = field.to_le_radix(256);\n        assert_eq(bytes, [3, 1, 0, 0, 0, 0, 0, 0]);\n        assert_eq(Field::from_le_bytes::<8>(bytes), field);\n    }\n    // docs:end:to_le_radix_example\n\n    #[test(should_fail_with = \"radix must be greater than 1\")]\n    fn test_to_le_radix_1() {\n        // this test should only fail in constrained mode\n        if !runtime::is_unconstrained() {\n            let field = 2;\n            let _: [u8; 8] = field.to_le_radix(1);\n        } else {\n            panic(f\"radix must be greater than 1\");\n        }\n    }\n\n    // TODO: Update this test to account for the Brillig restriction that the radix must be greater than 2\n    //#[test]\n    //fn test_to_le_radix_brillig_1() {\n    //    // this test should only fail in constrained mode\n    //    if runtime::is_unconstrained() {\n    //        let field = 1;\n    //        let out: [u8; 8] = field.to_le_radix(1);\n    //        crate::println(out);\n    //        let expected = [0; 8];\n    //        assert(out == expected, \"unexpected result\");\n    //    }\n    //}\n\n    #[test(should_fail_with = \"radix must be a power of 2\")]\n    fn test_to_le_radix_3() {\n        // this test should only fail in constrained mode\n        if !runtime::is_unconstrained() {\n            let field = 2;\n            let _: [u8; 8] = field.to_le_radix(3);\n        } else {\n            panic(f\"radix must be a power of 2\");\n        }\n    }\n\n    #[test]\n    fn test_to_le_radix_brillig_3() {\n        // this test should only fail in constrained mode\n        if runtime::is_unconstrained() {\n            let field = 1;\n            let out: [u8; 8] = field.to_le_radix(3);\n            let mut expected = [0; 8];\n            expected[0] = 1;\n            assert(out == expected, \"unexpected result\");\n        }\n    }\n\n    #[test(should_fail_with = \"radix must be less than or equal to 256\")]\n    fn test_to_le_radix_512() {\n        // this test should only fail in constrained mode\n        if !runtime::is_unconstrained() {\n            let field = 2;\n            let _: [u8; 8] = field.to_le_radix(512);\n        } else {\n            panic(f\"radix must be less than or equal to 256\")\n        }\n    }\n\n    #[test(should_fail_with = \"Field failed to decompose into specified 16 limbs\")]\n    unconstrained fn not_enough_limbs_brillig() {\n        let _: [u8; 16] = 0x100000000000000000000000000000000.to_le_bytes();\n    }\n\n    #[test(should_fail_with = \"Field failed to decompose into specified 16 limbs\")]\n    fn not_enough_limbs() {\n        let _: [u8; 16] = 0x100000000000000000000000000000000.to_le_bytes();\n    }\n\n    // TODO: Update this test to account for the Brillig restriction that the radix must be less than 512\n    //#[test]\n    //fn test_to_le_radix_brillig_512() {\n    //    // this test should only fail in constrained mode\n    //    if runtime::is_unconstrained() {\n    //        let field = 1;\n    //        let out: [u8; 8] = field.to_le_radix(512);\n    //        let mut expected = [0; 8];\n    //        expected[0] = 1;\n    //        assert(out == expected, \"unexpected result\");\n    //    }\n    //}\n\n    #[test]\n    unconstrained fn test_field_less_than() {\n        assert(field_less_than(0, 1));\n        assert(field_less_than(0, 0x100));\n        assert(field_less_than(0x100, 0 - 1));\n        assert(!field_less_than(0 - 1, 0));\n    }\n}\n","path":"std/field/mod.nr"},"50":{"source":"//! LaxStell `match_orders` circuit (SPEC sec 8.4).\n//!\n//! Proves two hidden orders are compatible and that the settlement notes,\n//! residual orders, and refund notes are correctly formed.\n//!\n//! Trading model:\n//!   * opposite sides, same (base, quote) pair, buy_price >= sell_price\n//!   * execution price = floor((buy_price + sell_price) / 2)   (midpoint)\n//!   * fill_amount     = min(buy_amount, sell_amount)\n//!   * quote_filled    = floor(fill_amount * exec_price / PRICE_SCALE)\n//!   * buyer receives `fill_amount` of the BASE asset\n//!   * seller receives `quote_filled` of the QUOTE asset\n//!\n//! Residual / refund policy (mapped to order A and order B, NOT buyer/seller):\n//!   * If an order's amount > fill_amount it is PARTIALLY filled: a residual\n//!     order (same side, same price, remaining amount) is created and NO refund\n//!     is issued for that order (its funds stay locked in the residual).\n//!   * If an order is FULLY filled (amount == fill_amount):\n//!       - a BUY order gets a quote-asset refund of its price improvement,\n//!         floor(amount*buy_price/SCALE) - quote_filled, when positive;\n//!       - a SELL order gets no refund (its locked base is fully delivered).\n//!\n//! KNOWN SIMPLIFICATION (MVP, documented in circuits/README.md): a partially\n//! filled order receives no price-improvement refund on its filled portion;\n//! that overpayment remains locked. See README \"Assumptions\".\n//!\n//! Public inputs (SHARED sec 7, IN THIS ORDER):\n//!   order_commitment_a, order_commitment_b, fill_note_buyer, fill_note_seller,\n//!   residual_order_a, residual_order_b, refund_note_a, refund_note_b\n\nuse dep::larel_lib as w;\n\n// Compute the (residual_commitment, refund_commitment) expected for ONE order.\n// `amount` and `price` must be range-checked to < 2^64 by the caller.\nfn settle_order(\n    side: Field,\n    price: Field,\n    amount: Field,\n    asset_base: Field,\n    asset_quote: Field,\n    owner_key: Field,\n    fill: Field,\n    quote_filled: Field,\n    residual_nonce: Field,\n    refund_blinding: Field,\n) -> (Field, Field) {\n    // fill = min(buy_amount, sell_amount) <= amount, so this is a non-negative\n    // 64-bit integer.\n    let residual_amount = amount - fill;\n    w::assert_64(residual_amount);\n\n    let mut residual = 0;\n    let mut refund = 0;\n\n    if residual_amount != 0 {\n        // Partially filled: re-commit the remaining order (same side & price).\n        residual = w::order_commitment(\n            side,\n            price,\n            residual_amount,\n            asset_base,\n            asset_quote,\n            owner_key,\n            residual_nonce,\n        );\n        // No refund on the partially filled side (MVP).\n    } else {\n        // Fully filled.\n        if side == 0 {\n            // BUY: refund quote-asset price improvement (exec_price <= buy_price).\n            let locked = w::mul_div_price(amount, price);\n            let refund_amount = locked - quote_filled; // >= 0\n            if refund_amount != 0 {\n                refund =\n                    w::note_commitment(asset_quote, refund_amount, owner_key, refund_blinding);\n            }\n        }\n        // SELL fully filled: locked base fully delivered, no refund.\n    }\n\n    (residual, refund)\n}\n\nfn main(\n    // ---- public inputs (order is load-bearing: SHARED sec 7) ----\n    order_commitment_a: pub Field,\n    order_commitment_b: pub Field,\n    fill_note_buyer: pub Field,\n    fill_note_seller: pub Field,\n    residual_order_a: pub Field,\n    residual_order_b: pub Field,\n    refund_note_a: pub Field,\n    refund_note_b: pub Field,\n    // ---- private inputs: order A ----\n    a_side: Field,\n    a_price: Field,\n    a_amount: Field,\n    a_asset_base: Field,\n    a_asset_quote: Field,\n    a_owner_key: Field,\n    a_nonce: Field,\n    // ---- private inputs: order B ----\n    b_side: Field,\n    b_price: Field,\n    b_amount: Field,\n    b_asset_base: Field,\n    b_asset_quote: Field,\n    b_owner_key: Field,\n    b_nonce: Field,\n    // ---- private inputs: blindings / nonces for new notes ----\n    buyer_fill_blinding: Field,\n    seller_fill_blinding: Field,\n    residual_a_nonce: Field,\n    residual_b_nonce: Field,\n    refund_a_blinding: Field,\n    refund_b_blinding: Field,\n) {\n    // 1. Both order commitments must reproduce the public values.\n    let ca = w::order_commitment(\n        a_side, a_price, a_amount, a_asset_base, a_asset_quote, a_owner_key, a_nonce,\n    );\n    assert(ca == order_commitment_a, \"order A commitment mismatch\");\n    let cb = w::order_commitment(\n        b_side, b_price, b_amount, b_asset_base, b_asset_quote, b_owner_key, b_nonce,\n    );\n    assert(cb == order_commitment_b, \"order B commitment mismatch\");\n\n    // 2. Sides must be boolean and opposite.\n    assert(a_side * (1 - a_side) == 0, \"A side must be 0 or 1\");\n    assert(b_side * (1 - b_side) == 0, \"B side must be 0 or 1\");\n    assert(a_side != b_side, \"orders must be opposite sides\");\n\n    // 3. Same trading pair.\n    assert(a_asset_base == b_asset_base, \"base asset mismatch\");\n    assert(a_asset_quote == b_asset_quote, \"quote asset mismatch\");\n\n    // Range checks before integer reasoning.\n    w::assert_64(a_price);\n    w::assert_64(b_price);\n    w::assert_64(a_amount);\n    w::assert_64(b_amount);\n\n    // 4. Identify buyer and seller.\n    let a_is_buy = a_side == 0;\n    let buy_price = if a_is_buy { a_price } else { b_price };\n    let sell_price = if a_is_buy { b_price } else { a_price };\n    let buy_amount = if a_is_buy { a_amount } else { b_amount };\n    let sell_amount = if a_is_buy { b_amount } else { a_amount };\n    let buyer_key = if a_is_buy { a_owner_key } else { b_owner_key };\n    let seller_key = if a_is_buy { b_owner_key } else { a_owner_key };\n\n    // 5. Price compatibility.\n    assert(w::geq64(buy_price, sell_price), \"prices incompatible\");\n\n    // 6. Execution price = floor midpoint.\n    let exec_price = w::midpoint(buy_price, sell_price);\n\n    // 7. Fill amount + quote filled.\n    let fill = w::min64(buy_amount, sell_amount);\n    let quote_filled = w::mul_div_price(fill, exec_price);\n    w::assert_64(fill);\n    w::assert_64(quote_filled);\n\n    // 8. Settlement notes (pair-wide assets; both orders agree on base/quote).\n    let asset_base = a_asset_base;\n    let asset_quote = a_asset_quote;\n    let buyer_note = w::note_commitment(asset_base, fill, buyer_key, buyer_fill_blinding);\n    assert(buyer_note == fill_note_buyer, \"buyer fill note mismatch\");\n    let seller_note = w::note_commitment(asset_quote, quote_filled, seller_key, seller_fill_blinding);\n    assert(seller_note == fill_note_seller, \"seller fill note mismatch\");\n\n    // 9. Residual orders + refunds, computed per order and mapped to A / B.\n    let (res_a, ref_a) = settle_order(\n        a_side, a_price, a_amount, asset_base, asset_quote, a_owner_key, fill, quote_filled,\n        residual_a_nonce, refund_a_blinding,\n    );\n    assert(res_a == residual_order_a, \"residual A mismatch\");\n    assert(ref_a == refund_note_a, \"refund A mismatch\");\n\n    let (res_b, ref_b) = settle_order(\n        b_side, b_price, b_amount, asset_base, asset_quote, b_owner_key, fill, quote_filled,\n        residual_b_nonce, refund_b_blinding,\n    );\n    assert(res_b == residual_order_b, \"residual B mismatch\");\n    assert(ref_b == refund_note_b, \"refund B mismatch\");\n}\n\n// ===========================================================================\n// Tests\n// ===========================================================================\n\n#[test]\nfn test_match_full_fill_with_buyer_refund() {\n    // A: BUY 10 base @ 2.0 ; B: SELL 10 base @ 1.0 -> exec 1.5, fill 10, quote 15\n    let ok_a = w::owner_key(7);\n    let ok_b = w::owner_key(9);\n    let oca = w::order_commitment(0, 20000000, 10, 0, 1, ok_a, 42);\n    let ocb = w::order_commitment(1, 10000000, 10, 0, 1, ok_b, 43);\n\n    let fnb = w::note_commitment(0, 10, ok_a, 701); // buyer gets 10 base\n    let fns = w::note_commitment(1, 15, ok_b, 702); // seller gets 15 quote\n    let rna = w::note_commitment(1, 5, ok_a, 703); // buyer refund 5 quote (20 - 15)\n\n    main(\n        oca, ocb, fnb, fns, 0, 0, rna, 0,\n        0, 20000000, 10, 0, 1, ok_a, 42,\n        1, 10000000, 10, 0, 1, ok_b, 43,\n        701, 702, 801, 802, 703, 804,\n    );\n}\n\n#[test]\nfn test_match_partial_fill_residual() {\n    // A: BUY 10 base @ 2.0 ; B: SELL 4 base @ 1.0 -> exec 1.5, fill 4, quote 6\n    let ok_a = w::owner_key(7);\n    let ok_b = w::owner_key(9);\n    let oca = w::order_commitment(0, 20000000, 10, 0, 1, ok_a, 42);\n    let ocb = w::order_commitment(1, 10000000, 4, 0, 1, ok_b, 43);\n\n    let fnb = w::note_commitment(0, 4, ok_a, 701); // buyer gets 4 base\n    let fns = w::note_commitment(1, 6, ok_b, 702); // seller gets 6 quote\n    // A is partially filled: residual buy order for 6 base @ 2.0, no refund\n    let res_a = w::order_commitment(0, 20000000, 6, 0, 1, ok_a, 801);\n\n    main(\n        oca, ocb, fnb, fns, res_a, 0, 0, 0,\n        0, 20000000, 10, 0, 1, ok_a, 42,\n        1, 10000000, 4, 0, 1, ok_b, 43,\n        701, 702, 801, 802, 703, 804,\n    );\n}\n\n#[test(should_fail_with = \"orders must be opposite sides\")]\nfn test_match_same_side_fails() {\n    let ok_a = w::owner_key(7);\n    let ok_b = w::owner_key(9);\n    // both BUY\n    let oca = w::order_commitment(0, 20000000, 10, 0, 1, ok_a, 42);\n    let ocb = w::order_commitment(0, 10000000, 10, 0, 1, ok_b, 43);\n    let fnb = w::note_commitment(0, 10, ok_a, 701);\n    let fns = w::note_commitment(1, 15, ok_a, 702);\n    main(\n        oca, ocb, fnb, fns, 0, 0, 0, 0,\n        0, 20000000, 10, 0, 1, ok_a, 42,\n        0, 10000000, 10, 0, 1, ok_b, 43,\n        701, 702, 801, 802, 703, 804,\n    );\n}\n\n#[test(should_fail_with = \"prices incompatible\")]\nfn test_match_price_incompatible_fails() {\n    // A: BUY @ 1.0 ; B: SELL @ 2.0 -> buy_price < sell_price\n    let ok_a = w::owner_key(7);\n    let ok_b = w::owner_key(9);\n    let oca = w::order_commitment(0, 10000000, 10, 0, 1, ok_a, 42);\n    let ocb = w::order_commitment(1, 20000000, 10, 0, 1, ok_b, 43);\n    let fnb = w::note_commitment(0, 10, ok_a, 701);\n    let fns = w::note_commitment(1, 15, ok_b, 702);\n    main(\n        oca, ocb, fnb, fns, 0, 0, 0, 0,\n        0, 10000000, 10, 0, 1, ok_a, 42,\n        1, 20000000, 10, 0, 1, ok_b, 43,\n        701, 702, 801, 802, 703, 804,\n    );\n}\n\n#[test(should_fail_with = \"buyer fill note mismatch\")]\nfn test_match_wrong_buyer_note_fails() {\n    let ok_a = w::owner_key(7);\n    let ok_b = w::owner_key(9);\n    let oca = w::order_commitment(0, 20000000, 10, 0, 1, ok_a, 42);\n    let ocb = w::order_commitment(1, 10000000, 10, 0, 1, ok_b, 43);\n    let bad_fnb = 123456; // wrong buyer settlement note\n    let fns = w::note_commitment(1, 15, ok_b, 702);\n    let rna = w::note_commitment(1, 5, ok_a, 703);\n    main(\n        oca, ocb, bad_fnb, fns, 0, 0, rna, 0,\n        0, 20000000, 10, 0, 1, ok_a, 42,\n        1, 10000000, 10, 0, 1, ok_b, 43,\n        701, 702, 801, 802, 703, 804,\n    );\n}\n","path":"/Users/em/web/folder/larel-main/circuits/noir/match_orders/src/main.nr"},"51":{"source":"//! LaxStell shared circuit library.\n//!\n//! Centralises every cryptographic invariant that MUST be byte-for-byte identical\n//! across the Noir circuits, the Soroban contract, and the TypeScript SDK.\n//! See SHARED.md (sec 3-5) -- this file is the in-circuit realisation of those rules.\n//!\n//! Poseidon2: BN254, state width t=4, RATE 3, HADES, S-box x^5, variable-length\n//! sponge with domain/IV = message_size << 64. The `poseidon` lib's\n//! `Poseidon2::hash(input, message_size)` takes the *number of field elements\n//! absorbed* as the second argument (NOT a capacity).\n\nuse dep::poseidon::poseidon2::Poseidon2;\n\n/// Merkle tree depth (SHARED sec 4 / 5).\npub global TREE_DEPTH: u32 = 20;\n\n/// Fixed-point price scale (SHARED sec 4): prices are multiplied by 10^7.\npub global PRICE_SCALE: Field = 10000000;\n\n// ---------------------------------------------------------------------------\n// Poseidon2 hash arities (SHARED sec 3)\n// ---------------------------------------------------------------------------\n\n/// 2-input Poseidon2 hash -- Merkle nodes, nullifiers, key derivation.\npub fn hash2(a: Field, b: Field) -> Field {\n    Poseidon2::hash([a, b], 2)\n}\n\n/// 4-input Poseidon2 hash -- balance note commitments.\npub fn hash4(a: Field, b: Field, c: Field, d: Field) -> Field {\n    Poseidon2::hash([a, b, c, d], 4)\n}\n\n/// 7-input Poseidon2 hash -- order commitments.\npub fn hash7(a: Field, b: Field, c: Field, d: Field, e: Field, f: Field, g: Field) -> Field {\n    Poseidon2::hash([a, b, c, d, e, f, g], 7)\n}\n\n// ---------------------------------------------------------------------------\n// Domain commitments (SHARED sec 4)\n// ---------------------------------------------------------------------------\n\n/// Balance-note commitment. Field order is fixed everywhere:\n/// (asset_id, amount, owner_key, blinding).\npub fn note_commitment(asset_id: Field, amount: Field, owner_key: Field, blinding: Field) -> Field {\n    hash4(asset_id, amount, owner_key, blinding)\n}\n\n/// Nullifier of a note: hash2(commitment, spending_key). Only the owner (who\n/// knows spending_key) can derive it; deterministic, so one note => one nullifier.\npub fn nullifier(commitment: Field, spending_key: Field) -> Field {\n    hash2(commitment, spending_key)\n}\n\n/// Public owner key derived from the secret spending key: hash2(spending_key, 0).\npub fn owner_key(spending_key: Field) -> Field {\n    hash2(spending_key, 0)\n}\n\n/// Order commitment. Field order is fixed:\n/// (side, price, amount, asset_base, asset_quote, owner_key, nonce).\npub fn order_commitment(\n    side: Field,\n    price: Field,\n    amount: Field,\n    asset_base: Field,\n    asset_quote: Field,\n    owner_key: Field,\n    nonce: Field,\n) -> Field {\n    hash7(side, price, amount, asset_base, asset_quote, owner_key, nonce)\n}\n\n// ---------------------------------------------------------------------------\n// Bounded integer arithmetic (SHARED: amounts/prices < 2^64)\n// ---------------------------------------------------------------------------\n//\n// IMPORTANT: a `Field as u64` / `as u128` cast in Noir TRUNCATES silently (it\n// does NOT range-check). Every value fed into the helpers below MUST first be\n// constrained with `assert_64` (or otherwise proven < 2^64), otherwise the\n// integer reasoning is unsound. The circuits call `assert_64` on every amount\n// and price before using these helpers.\n\n/// Range-check a Field to 64 bits. Wrapper around `assert_max_bit_size::<64>()`.\npub fn assert_64(x: Field) {\n    x.assert_max_bit_size::<64>();\n}\n\n/// `a >= b`, assuming both already range-checked to < 2^64.\npub fn geq64(a: Field, b: Field) -> bool {\n    (a as u64) >= (b as u64)\n}\n\n/// `a > b`, assuming both already range-checked to < 2^64.\npub fn gt64(a: Field, b: Field) -> bool {\n    (a as u64) > (b as u64)\n}\n\n/// `min(a, b)` as a Field, assuming both already range-checked to < 2^64.\npub fn min64(a: Field, b: Field) -> Field {\n    let au = a as u64;\n    let bu = b as u64;\n    (if au < bu { au } else { bu }) as Field\n}\n\n/// Floor of the midpoint `(a + b) / 2`, assuming a, b < 2^64.\n/// Sum < 2^65 fits in u128; division is integer/floor (SHARED: midpoint, noted\n/// as floor-rounded in the README).\npub fn midpoint(a: Field, b: Field) -> Field {\n    let s: u128 = (a as u128) + (b as u128);\n    (s / 2) as Field\n}\n\n/// Floor of `(a * b) / PRICE_SCALE`, assuming a, b < 2^64.\n/// The product a*b < 2^128 fits in u128; division is integer/floor.\npub fn mul_div_price(a: Field, b: Field) -> Field {\n    let prod: u128 = (a as u128) * (b as u128);\n    let scale: u128 = 10000000; // PRICE_SCALE\n    (prod / scale) as Field\n}\n\n// ---------------------------------------------------------------------------\n// Merkle proof verification (SHARED sec 5)\n// ---------------------------------------------------------------------------\n\n/// Enforce that `bit` is boolean (0 or 1).\nfn constrain_bit(bit: Field) {\n    assert(bit * (1 - bit) == 0, \"merkle path bit must be 0 or 1\");\n}\n\n/// Recompute the Merkle root from `leaf` up the path.\n///\n/// Bit ordering (SHARED sec 5):\n///   * bit == 0 -> current node is LEFT  -> hash2(cur, sibling)\n///   * bit == 1 -> current node is RIGHT -> hash2(sibling, cur)\npub fn compute_merkle_root(\n    leaf: Field,\n    path_siblings: [Field; TREE_DEPTH],\n    path_bits: [Field; TREE_DEPTH],\n) -> Field {\n    let mut cur = leaf;\n    for i in 0..TREE_DEPTH {\n        let sib = path_siblings[i];\n        let bit = path_bits[i];\n        constrain_bit(bit);\n        if bit == 0 {\n            cur = hash2(cur, sib);\n        } else {\n            cur = hash2(sib, cur);\n        }\n    }\n    cur\n}\n\n// ---------------------------------------------------------------------------\n// Library self-tests (commitment helpers + Merkle ordering)\n// ---------------------------------------------------------------------------\n\n#[test]\nfn test_owner_key_matches_hash2() {\n    let sk = 12345;\n    assert(owner_key(sk) == hash2(sk, 0));\n}\n\n#[test]\nfn test_note_commitment_then_nullifier_roundtrip() {\n    let ok = owner_key(7);\n    let c = note_commitment(0, 100, ok, 999);\n    let nf = nullifier(c, 7);\n    // Deterministic: same inputs => same nullifier.\n    assert(nf == hash2(c, 7));\n    assert(nf == nullifier(note_commitment(0, 100, ok, 999), 7));\n}\n\n#[test]\nfn test_order_commitment_field_order() {\n    let ok = owner_key(3);\n    let oc = order_commitment(1, 50000000, 10, 0, 1, ok, 42);\n    assert(oc == hash7(1, 50000000, 10, 0, 1, ok, 42));\n}\n\n#[test]\nfn test_merkle_root_left_and_right_differ() {\n    let leaf = 111;\n    let sib = 222;\n    let mut siblings = [0; TREE_DEPTH];\n    siblings[0] = sib;\n    let bits_left = [0; TREE_DEPTH];\n    let mut bits_right = [0; TREE_DEPTH];\n    bits_right[0] = 1;\n\n    let root_left = compute_merkle_root(leaf, siblings, bits_left);\n    let root_right = compute_merkle_root(leaf, siblings, bits_right);\n\n    // Level-0 node differs by orientation; the two roots must not coincide.\n    assert(root_left != root_right);\n}\n\n#[test(should_fail_with = \"merkle path bit must be 0 or 1\")]\nfn test_merkle_rejects_non_boolean_bit() {\n    let mut bits = [0; TREE_DEPTH];\n    bits[0] = 2; // not a bit\n    let _ = compute_merkle_root(5, [0; TREE_DEPTH], bits);\n}\n\n#[test]\nfn test_int_helpers() {\n    assert(midpoint(20000000, 10000000) == 15000000);\n    assert(midpoint(7, 4) == 5); // floor((7+4)/2) = 5\n    assert(mul_div_price(10, 20000000) == 20); // 10 * 2.0\n    assert(mul_div_price(3, 15000000) == 4); // floor(45000000/10^7) = 4\n    assert(min64(10, 4) == 4);\n    assert(min64(4, 10) == 4);\n    assert(geq64(100, 100));\n    assert(geq64(101, 100));\n    assert(!geq64(99, 100));\n    assert(gt64(101, 100));\n    assert(!gt64(100, 100));\n}\n","path":"/Users/em/web/folder/larel-main/circuits/noir/larel_lib/src/lib.nr"},"59":{"source":"use std::default::Default;\nuse std::hash::Hasher;\n\ncomptime global RATE: u32 = 3;\n\npub struct Poseidon2 {\n    cache: [Field; 3],\n    state: [Field; 4],\n    cache_size: u32,\n    squeeze_mode: bool, // 0 => absorb, 1 => squeeze\n}\n\nimpl Poseidon2 {\n    #[no_predicates]\n    pub fn hash<let N: u32>(input: [Field; N], message_size: u32) -> Field {\n        Poseidon2::hash_internal(input, message_size)\n    }\n\n    pub(crate) fn new(iv: Field) -> Poseidon2 {\n        let mut result =\n            Poseidon2 { cache: [0; 3], state: [0; 4], cache_size: 0, squeeze_mode: false };\n        result.state[RATE] = iv;\n        result\n    }\n\n    fn perform_duplex(&mut self) {\n        // add the cache into sponge state\n        for i in 0..RATE {\n            // We effectively zero-pad the cache by only adding to the state\n            // cache that is less than the specified `cache_size`\n            if i < self.cache_size {\n                self.state[i] += self.cache[i];\n            }\n        }\n        self.state = crate::poseidon2_permutation(self.state, 4);\n    }\n\n    fn absorb(&mut self, input: Field) {\n        assert(!self.squeeze_mode);\n        if self.cache_size == RATE {\n            // If we're absorbing, and the cache is full, apply the sponge permutation to compress the cache\n            self.perform_duplex();\n            self.cache[0] = input;\n            self.cache_size = 1;\n        } else {\n            // If we're absorbing, and the cache is not full, add the input into the cache\n            self.cache[self.cache_size] = input;\n            self.cache_size += 1;\n        }\n    }\n\n    fn squeeze(&mut self) -> Field {\n        assert(!self.squeeze_mode);\n        // If we're in absorb mode, apply sponge permutation to compress the cache.\n        self.perform_duplex();\n        self.squeeze_mode = true;\n\n        // Pop one item off the top of the permutation and return it.\n        self.state[0]\n    }\n\n    fn hash_internal<let N: u32>(input: [Field; N], in_len: u32) -> Field {\n        let two_pow_64 = 18446744073709551616;\n        let iv: Field = (in_len as Field) * two_pow_64;\n        let mut sponge = Poseidon2::new(iv);\n        for i in 0..input.len() {\n            if i < in_len {\n                sponge.absorb(input[i]);\n            }\n        }\n        sponge.squeeze()\n    }\n}\n\npub struct Poseidon2Hasher {\n    _state: [Field],\n}\n\nimpl Hasher for Poseidon2Hasher {\n    fn finish(self) -> Field {\n        let iv: Field = (self._state.len() as Field) * 18446744073709551616; // iv = (self._state.len() << 64)\n        let mut sponge = Poseidon2::new(iv);\n        for i in 0..self._state.len() {\n            sponge.absorb(self._state[i]);\n        }\n        sponge.squeeze()\n    }\n\n    fn write(&mut self, input: Field) {\n        self._state = self._state.push_back(input);\n    }\n}\n\nimpl Default for Poseidon2Hasher {\n    fn default() -> Self {\n        Poseidon2Hasher { _state: &[] }\n    }\n}\n","path":"/Users/em/nargo/github.com/noir-lang/poseidon/v0.2.0/src/poseidon2.nr"}},"names":["main"],"brillig_names":["directive_invert","directive_integer_quotient"]}