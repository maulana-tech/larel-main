[package]
name = "larel-swap-router"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
soroban-sdk = { version = "26.0.1", features = ["alloc"] }
soroban-poseidon = "26.0.0"

[lib]
crate-type = ["cdylib", "rlib"]
